Development and testing completed, awaiting PR review and merge.

PR: <PR_URL>

Suites (cloud session, 2026-09-25; no API key here, no live calls):
- MVP1 `src/Loan Manager/tests`: 297 passed, 1 skipped (the `llm`-marked live test; skips without OPENROUTER_API_KEY) — baseline 224
- infrastructure/llm + application/agent/llm_port.py coverage 100%
- root tests/unit 241 passed 2 skipped; integration 17 passed 1 skipped; lint-imports 3 kept

Scope correction vs row text: provider is OpenRouter (ARB D-4a), `OPENROUTER_API_KEY`, `qwen/qwen-2.5-72b-instruct` — not Groq. prices.json lives in infrastructure/llm/ (data/* is gitignored) and is deliberately EMPTY: no per-token price is evidenced, so cost_usd is None (never 0) unless the provider reports it.

Review: 3 fix cycles (the 3rd approved by the operator) — egress fidelity, error-contract, settings data-loss and free-tier cost findings all fixed.

Live acceptance ("real call returns a Completion") NOT executed here — needs your key locally: `OPENROUTER_API_KEY=... pytest -m llm "src/Loan Manager/tests/unit/infrastructure/llm/test_openrouter_live.py"`.

DEBT: port types not validated (non-int tokens, dict content); generic except wraps ToolCall construction; settings_tab pre-existing: custom_colours null → TypeError, colour-only save rewrites theme from stale memory, theme apply can overwrite colours written by another process, write_text not atomic; boundary lint misses `from .openai_compat_client import openai`, `sys.modules`, `__builtins__[...]`, `**{"_http_client":..}`.

RTK gain: not measurable — rtk not installed in this cloud environment (KCH-254).
