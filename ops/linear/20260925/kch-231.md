Development and testing completed, awaiting PR review and merge.

PR: https://github.com/K4nishk/FinHive/pull/45

Suites (cloud session, 2026-09-25):
- MVP1 `src/Loan Manager/tests`: 272 passed (baseline 224)
- root tests/unit 241 passed 2 skipped; integration 17 passed 1 skipped; lint-imports 3 kept
- `src/Loan Manager/data/` identical before and after the full suite (no loans.db created)

Delivered (re-scoped per 9fa1057: synthetic, encrypted SQLite demo DB):
- `FINHIVE_DB_PATH` selects the database (blank/whitespace → default data/loans.db); startup logs the resolved path
- SQLite connections use WAL (connect listener; required by the KCH-241 QThread worker); BackupService uses the sqlite3 backup API so WAL rows are included, and refuses a missing source
- `python -m loan_manager.infrastructure.seed --db <path> [--today YYYY-MM-DD] [--replace]` seeds 27 synthetic loans (15 MVP1 sample rows + sharma group, iyer chem / meera iyer, bg10/bg13, undated, future-giving, paid-off, one ≥1 crore amount) through the encrypting repository only; refuses data/loans.db and the FINHIVE_DB_PATH database (path, symlink or hardlink); checks the key before deleting anything
- Test safety: every data path redirected to tmp; the session fails if the real data/loans.db{,-wal,-shm} or data/settings.json change

Also: the CI MVP1 job installs libegl1 and runs Qt offscreen (same fix as KCH-234 / PR #44; needed because tests now import a presentation module).

HOW TO USE (your machine): `cd "src/Loan Manager" && python -m loan_manager.infrastructure.seed --db data/demo.db` then `FINHIVE_DB_PATH=data/demo.db ./run_mac.sh`.

DEBT: foreign_keys pragma off; settings_tab shows DATA_DIR not the active DB; RecomputeAllStatuses rewrites seeded statuses on launch (by design); encrypt_existing_rows.py backup still uses copy2 (not WAL-safe) — KCH-247 must fix before real data.

RTK gain: not measurable — rtk not installed in this cloud environment (KCH-254).
