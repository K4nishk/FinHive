Development and testing completed, awaiting PR review and merge.

PR: <PR_URL>

Suites (cloud session, 2026-09-25):
- MVP1 `src/Loan Manager/tests`: 269 passed (baseline 224)
- entity_resolver.py statement+branch coverage 100% (166 stmts, 60 branches); build_entity_resolver.py 100%
- root tests/unit: 241 passed, 2 skipped (baseline); lint-imports 3 kept

Delivered: pure-domain EntityResolver over borrower_name / borrower_group / depositor_name / depositor_group (stdlib difflib + OSA distance). Returns the ORIGINAL stored value (what the exact blind-index filter needs); never substring; ambiguity → top2, never a guess; "group"/"grp" restricts to group fields; `Resolution.exact` marks exact resolves.

CONTRACT FOR KCH-238/239: act directly only on `Resolution.exact`; any non-exact resolve (typos, and name pairs like amit shah↔amita shah) must be confirmed with the user ("did you mean X?") before use.

DEBT: GetAutocompleteValues hardcodes active_only=True (get_autocomplete.py:18) → paid-off names unresolvable; short-surname single-substitution typos (≤6 letters) never resolve; "ramlal"↔"ram lal" exact when only one stored; hint-word queries ("sharma group") never exact → always confirmed; sqlalchemy_loan_repo.py:88 docstring says application layer (it's domain).

RTK gain: not measurable — rtk not installed in this cloud environment (KCH-254).
