Development and testing completed, awaiting PR review and merge.

PR: <PR_URL>

Closes KCH-230 (row 5) and absorbed KCH-114.

Suites (cloud session, 2026-09-25):
- MVP1 `src/Loan Manager/tests`: 230 passed (baseline 224; +6 in tests/integration/test_encryption_at_rest.py)
- root tests/unit: 241 passed, 2 skipped (baseline)
- root tests/integration (disposable Postgres): 17 passed, 1 skipped (baseline)
- lint-imports: 3 kept

Proven on a real SQLite file (WAL + PERSIST journal), data written only through the encrypting repositories/use cases:
- every field of loans / loan_history / report_records round-trips exactly (independent expected values), incl. null due_date
- no plaintext name/group/amount (incl. the 4 derived amounts) in db, -wal or -journal — all 18 encrypted column instances mutation-checked
- tampered ciphertext → DecryptionError via the repository; no index on any amount column; blind index deterministic + exact filter

[REVIEW REQUIRED] KCH-114 "wire" clause: N/A while the app is SQLite-only (no network hop; Postgres deferred by ARB D-16).

Docs: FINHIVE_MASTER_KEY → FINHIVE_KEY_VERSION + FINHIVE_MASTER_KEY_V<n> (docs/MVP1_1_ASK_FINHIVE.md, ARB_DECISIONS.md, ops/gen_m11_csv.py → regenerated CSV).

DEBT: LoanHistoryRepository has no read method (tests read the ORM row).

RTK gain: not measurable — rtk not installed in this cloud environment (KCH-254).
