Development and testing completed, awaiting PR review and merge.

PR: https://github.com/K4nishk/FinHive/pull/46

Suites (cloud session; branch based on integration/m11-c1 = wave 1 merged):
- MVP1 `src/Loan Manager/tests`: 525 passed, 1 skipped (the live llm test, no key)
- application/agent coverage 100%; root tests/unit 247 passed 2 skipped; lint-imports 3 kept

Delivered: six READ tool handlers + build_read_registry(uow_factory, clock). query_loans rejects any group that is not a stored exact value BEFORE querying (UNRESOLVED_ENTITY → "call resolve_entity first"), never a confident zero; status always from StatusEngine at Clock.today (never loans.status); undated overdue loans counted but never in max_days_overdue (with a note); paidoff → UNSUPPORTED_STATUS; no names in its projection. resolve_entity follows the KCH-236 exact/confirm contract. Portfolio ranks ACTIVE+OVERDUE exposure numerically. format_inr uses Indian grouping (₹1,85,000.00).
Verified by the reviewer against a real seeded encrypted DB (all six tools correct).

[REVIEW REQUIRED] "this quarter" uses the Indian financial year (Apr–Mar, Jul–Sep = Q2).
Note for KCH-248/249: demo_fixture stores "sharma group" as a real borrower_group.

DEBT: no read-by-ref use case (calculate_interest scans active loans); Slug membership for PROPOSE tools → KCH-243; names/amounts in tool output are tokenised by KCH-238, not here.

RTK gain: not measurable — rtk not installed in this cloud environment (KCH-254).
