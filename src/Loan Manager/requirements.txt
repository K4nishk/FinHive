PySide6>=6.8.0              # Python 3.13 support
SQLAlchemy>=2.0.0
alembic>=1.13.0
pydantic>=2.5.0             # Python 3.13 compatible
python-dateutil>=2.8.2
openpyxl>=3.1.2
pytest>=8.0.0
pytest-cov>=5.0.0
cryptography>=42.0.0        # AES-256-GCM + HKDF, via finhive.db (ARB D-15)
openai>=1.40,<3              # OpenAI-compatible client for OpenRouter (ARB D-4a);
                              # only infrastructure/llm/openai_compat_client.py may import it

# NOTE: the `finhive` package (finhive.db.encryption / .keys) is NOT listed here.
# A relative `-e ../..` in a requirements file resolves against pip's WORKING
# DIRECTORY, not against this file, so it installs the wrong path — or fails —
# depending on where the caller happens to stand. run_mac.sh and run_windows.bat
# install it explicitly with a path derived from the script's own location.
