PySide6>=6.8.0              # Python 3.13 support
SQLAlchemy>=2.0.0
alembic>=1.13.0
pydantic>=2.5.0             # Python 3.13 compatible
python-dateutil>=2.8.2
openpyxl>=3.1.2
pytest>=8.0.0
pytest-cov>=5.0.0
cryptography>=42.0.0        # AES-256-GCM + HKDF, via finhive.db (ARB D-15)

# Encryption at rest calls finhive.db.encryption and finhive.db.keys. Installed
# from the repository root as an editable package: without it `import finhive`
# succeeds only when the working directory happens to BE the repo root, which is
# how it silently "worked" before KCH-226 while `cryptography` was absent and
# every such import actually failed.
-e ../..
